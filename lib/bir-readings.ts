// Re-exported from lib/data/reports.ts (Prisma-backed) — kept as a thin
// shim so existing import paths (`@/lib/bir-readings`) continue to work.
export {
  startOfBusinessDay,
  getDailySalesAggregate,
  type DailySalesAggregate,
} from '@/lib/data/reports';
