/**
 * Canonical 1Pos brand default primary (teal). Keep in sync with:
 * - `app/globals.css` :root --primary-color
 * - `getDefaultTenantSettings().primaryColor` in lib/currency.ts
 */
export const BRAND_PRIMARY_HEX = '#35979c' as const;
