services:
  postgres:
    image: postgres:16-alpine
    container_name: pos-system-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: pos
      POSTGRES_PASSWORD: pos
      POSTGRES_DB: pos_system
    ports:
      - '5432:5432'
    volumes:
      - pos_postgres_data:/var/lib/postgresql/data

volumes:
  pos_postgres_data:
